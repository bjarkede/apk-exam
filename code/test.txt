// error testing:
}

// keyword using compile-time array generation
print(fib(31))

// run-time implementation
// higher than runtimefib(31) causes bad_alloc...
let runtimefib(n) = 
	if n < 2 then n else runtimefib(n - 1) + runtimefib(n - 2) 
	in print(runtimefib(31)) 
end
